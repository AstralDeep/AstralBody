<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext (non-TLS) traffic is allowed ONLY to the local dev orchestrator:
  10.0.2.2 is the host machine's loopback as seen from the Android emulator;
  localhost/127.0.0.1 cover a device port-forwarded over adb. Everything else
  stays HTTPS/WSS-only (production points at wss://sandbox.ai.uky.edu, which is
  TLS and unaffected by this allowance).
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
