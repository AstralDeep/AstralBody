# Orchestrator Configuration
HOST=0.0.0.0
ORCHESTRATOR_PORT=8001
AGENT_PORT=8003

# Public-facing URL (used for A2A agent cards and OAuth callbacks)
# Local dev: http://127.0.0.1:8001  |  Production: https://your-domain.com
BACKEND_PUBLIC_URL=http://localhost:8001
PUBLIC_BASE_URL=http://localhost:8001

# OpenAI-compatible LLM
OPENAI_API_KEY=
OPENAI_BASE_URL=
LLM_MODEL=

# Auto-start agents
AUTO_START_AGENTS=true

# Default owner for newly registered agents (auto-assigned on first connect)
DEFAULT_AGENT_OWNER= # email address, e.g. test@test.com

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=astralbody
DB_USER=astral
DB_PASSWORD=astral_dev

# Debug
DEBUG=true

# Feature Flags (true/false — controls new orchestrator features)
FF_DENIAL_LOOP_DETECTION=true
FF_TOOL_CONCURRENCY_SAFETY=true
FF_MESSAGE_COMPACTION=false
FF_PROGRESS_STREAMING=false
FF_HOOK_SYSTEM=false
FF_TASK_STATE_MACHINE=false
FF_COORDINATOR_MODE=false
FF_KNOWLEDGE_SYNTHESIS=false
FF_LIVE_STREAMING=false
FF_TOOL_STREAMING=true

# Knowledge Synthesis "Dreamer" LLM (background learning from tool interactions)
# Any OpenAI-compatible endpoint will work. Defaults below assume local Ollama
# with a small CPU-friendly model (qwen2.5:0.5b = ~400MB). Docker Compose
# auto-pulls KNOWLEDGE_LLM_MODEL on first start.
# Examples:
#   Ollama:    http://localhost:11434/v1   (models: qwen2.5:0.5b, llama3.2:1b, ...)
#   LM Studio: http://localhost:1234/v1
#   Remote:    https://your-openai-compatible-host/v1   (set KNOWLEDGE_LLM_API_KEY too)
KNOWLEDGE_LLM_BASE_URL=http://localhost:11434/v1
KNOWLEDGE_LLM_API_KEY=ollama
KNOWLEDGE_LLM_MODEL=gemma4:e2b
KNOWLEDGE_SYNTHESIS_INTERVAL=1800
KNOWLEDGE_MIN_INTERACTIONS=20

# Keycloak Authentication
VITE_KEYCLOAK_AUTHORITY= # e.g. https://iam.ai.uky.edu/realms/YourRealm
VITE_KEYCLOAK_CLIENT_ID=
KEYCLOAK_CLIENT_SECRET=
AGENT_SERVICE_CLIENT_ID=
AGENT_SERVICE_CLIENT_SECRET=

# Mock Auth (for development — set to true to skip Keycloak and use a dev user)
VITE_USE_MOCK_AUTH=true

# Speaches.ai (STT/TTS)
SPEACHES_URL=http://your-speaches-instance:4958

# Keycloak Test Credentials (for integration/E2E testing)
KEYCLOAK_TEST_USER=
KEYCLOAK_TEST_PASSWORD=

# Audit log — server-held HMAC secret used for the
# hash chain and for payload digests. MUST be set to a
# high-entropy value before deploying outside dev.
AUDIT_HMAC_SECRET=dev-audit-hmac-secret-change-me-in-prod
AUDIT_HMAC_KEY_ID=k1