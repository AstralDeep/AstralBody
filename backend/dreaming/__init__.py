"""Background memory consolidation ("dreaming") for feature 025.

A per-user, default-on (opt-out) sweep that promotes high-signal, recurring,
non-PHI short-term signals into durable memory and keeps a reviewable trail.
Rides on the scheduler module.
"""
