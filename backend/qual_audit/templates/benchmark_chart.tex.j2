% Auto-generated by AstralBody Academic Testing Suite
% Transport Latency Comparison — SSE vs WebSocket
\begin{figure}[H]
\centering
\begin{tikzpicture}
\begin{axis}[
    ybar,
    bar width=15pt,
    xlabel={Metric},
    ylabel={Latency (ms)},
    symbolic x coords={Mean, Median, P95, P99},
    xtick=data,
    legend style={at={(0.5,-0.25)}, anchor=north, legend columns=2},
    nodes near coords,
    nodes near coords align={vertical},
    ymin=0,
    width=0.9\textwidth,
    height=8cm,
    every node near coord/.append style={font=\tiny},
]
<% if metrics %>
\addplot coordinates {
    (Mean, << metrics.get('sse_mean_ms', 0) >>)
    (Median, << metrics.get('sse_median_ms', 0) >>)
    (P95, << metrics.get('sse_p95_ms', 0) >>)
    (P99, << metrics.get('sse_p99_ms', 0) >>)
};
\addlegendentry{SSE (HTTP)}

\addplot coordinates {
    (Mean, << metrics.get('ws_mean_ms', 0) >>)
    (Median, << metrics.get('ws_median_ms', 0) >>)
    (P95, << metrics.get('ws_p95_ms', 0) >>)
    (P99, << metrics.get('ws_p99_ms', 0) >>)
};
\addlegendentry{WebSocket}
<% else %>
% No benchmark data available — run transport comparison tests first
\addplot coordinates {(Mean, 0) (Median, 0) (P95, 0) (P99, 0)};
\addlegendentry{SSE (no data)}
\addplot coordinates {(Mean, 0) (Median, 0) (P95, 0) (P99, 0)};
\addlegendentry{WebSocket (no data)}
<% endif %>
\end{axis}
\end{tikzpicture}
\caption{Transport Latency Comparison — SSE vs WebSocket (N=100)}
\label{fig:transport_latency}
\end{figure}
